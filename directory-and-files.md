# Commands to manage directory and files

## Create directory with sub-directories
`mkdir -p /data/backup/something`

## Delete all sub-directories into directory
`sudo rm -r -- ./*/`

## ls by file extension
`ls *.crt; ls *.pem`